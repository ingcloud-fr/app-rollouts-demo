# Installation Nginx avec Helm
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ingress-nginx-prod
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
  finalizers:
    - resources-finalizer.argocd.argoproj.io    
spec:
  project: default # project-infra
  source:
    repoURL: https://kubernetes.github.io/ingress-nginx
    chart: ingress-nginx
    targetRevision: 4.13.2
    helm:
      values: |
        controller:
          ingressClassResource:
            name: nginx-prod
            controllerValue: "k8s.io/ingress-nginx"
          ingressClass: nginx-prod
          ingressClassByName: true
          watchIngressWithoutClass: false
          publishService:
            enabled: true
            pathOverride: "ingress-nginx-prod/ingress-nginx-prod-controller"
          electionID: ingress-controller-leader-nginx-prod
          extraArgs:
            watch-namespace-selector: "env=prod"
          service:
            type: LoadBalancer
  destination:
    server: https://kubernetes.default.svc
    namespace: ingress-nginx-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true  