apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rollouts-active
  namespace: rollouts-demo
spec:
  ingressClassName: nginx
  rules:
    - host: rollouts-demo.ingcloud.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rollouts-active
                port:
                  number: 80
