apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-preview
spec:
  ingressClassName: nginx
  rules:
    - host: CHANGE-preview-app.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: svc-preview
                port:
                  number: 80
