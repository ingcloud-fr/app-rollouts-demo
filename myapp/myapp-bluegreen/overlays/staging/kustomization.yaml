apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: myapp-staging
images:
- name: argoproj/rollouts-demo
  newTag: yellow
resources:
  - ../../base
  - namespace.yaml # Pour la création
patches:
  - target:
      group: networking.k8s.io
      version: v1
      kind: Ingress
      name: ingress-active
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: staging-myapp.ingcloud.site
  - target:
      group: networking.k8s.io
      version: v1
      kind: Ingress
      name: ingress-preview
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: staging-myapp-preview.ingcloud.site

